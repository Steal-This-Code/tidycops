% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardized_incidents.R
\name{get_city_incidents}
\alias{get_city_incidents}
\title{Fetch City Incident Data (Full City Schema)}
\usage{
get_city_incidents(
  city,
  start_date = NULL,
  end_date = NULL,
  last_n_days = NULL,
  limit = 1000,
  select = NULL,
  where = NULL,
  clean = TRUE,
  ...
)
}
\arguments{
\item{city}{City key. See [list_supported_incident_cities()].}

\item{start_date, end_date}{Optional date bounds in `YYYY-MM-DD` format or as
`Date` objects.}

\item{last_n_days}{Optional. Positive integer shortcut that sets
`start_date`/`end_date` to the last `n` calendar days ending today.
Cannot be combined with `start_date` or `end_date`.}

\item{limit}{Maximum number of rows to return after combining source chunks.}

\item{select}{Optional raw source columns to request. If `NULL`, all source
columns are requested.}

\item{where}{Optional provider-specific filter clause. When supplied for a
city with multiple backing sources, an error is raised.}

\item{clean}{If `TRUE` (default), trim whitespace and convert empty strings
to `NA` for character columns.}

\item{...}{Additional query parameters passed to the provider API
(Socrata or ArcGIS).}
}
\value{
A tibble containing city raw columns plus adapter metadata columns:
  `city`, `city_display`, `source_id`, `source_name`, `source_dataset`,
  `source_url`, `city_incident_date`, and `city_reported_date`.
}
\description{
Downloads all available incident columns for one supported city adapter.
This path is useful when you want the city's full exposed schema for local
analysis in `dplyr`, `data.table`, `sf`, or other packages, without reducing
to cross-city standardized fields.
}
