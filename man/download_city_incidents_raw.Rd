% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_utils.R
\name{download_city_incidents_raw}
\alias{download_city_incidents_raw}
\title{Download Raw City Incident Data}
\usage{
download_city_incidents_raw(
  city = "dallas",
  start_date = NULL,
  end_date = NULL,
  last_n_days = NULL,
  limit = Inf,
  select = NULL,
  where = NULL,
  ...
)
}
\arguments{
\item{city}{City key. Defaults to `"dallas"`.}

\item{start_date}{Optional. A character string in `'YYYY-MM-DD'` format or a
`Date` object specifying the minimum incident date (inclusive).}

\item{end_date}{Optional. A character string in `'YYYY-MM-DD'` format or a
`Date` object specifying the maximum incident date (inclusive).}

\item{last_n_days}{Optional. Positive integer shortcut that sets
`start_date`/`end_date` to the last `n` calendar days ending today.
Cannot be combined with `start_date` or `end_date`.}

\item{limit}{The maximum number of raw records to return. Defaults to `Inf`.}

\item{select}{A character vector specifying which columns to retrieve.
If `NULL` (default), all columns are retrieved.}

\item{where}{An optional provider-specific filter clause.}

\item{...}{Additional API query parameters.}
}
\value{
A tibble containing untouched incident payload rows.
}
\description{
Downloads untouched incident payload rows for any supported city adapter.
This is a convenience wrapper around incident-only raw retrieval that avoids
Dallas-specific naming.
}
