% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardized_incidents.R
\name{get_standardized_incidents}
\alias{get_standardized_incidents}
\title{Fetch Standardized Incident Data}
\usage{
get_standardized_incidents(
  city,
  start_date = NULL,
  end_date = NULL,
  last_n_days = NULL,
  offense = NULL,
  offense_category = NULL,
  neighborhood = NULL,
  zip_code = NULL,
  beat = NULL,
  district = NULL,
  division = NULL,
  disposition = NULL,
  limit = 1000,
  select = NULL,
  where = NULL,
  output = c("both", "comparable"),
  as_sf = FALSE,
  ...
)
}
\arguments{
\item{city}{City key. Currently supports `"dallas"`, `"san_francisco"`,
`"cincinnati"`, `"providence"`, `"chicago"`, `"cleveland"`,
`"rochester"`, `"seattle"`, `"boston"`, `"washington_dc"`,
`"kansas_city"`, `"gainesville"`, `"hartford"`, `"houston"`,
`"new_orleans"`,
`"fort_lauderdale"`, `"naperville"`, `"denver"`, `"detroit"`,
`"indianapolis"`, `"minneapolis"`, `"grand_rapids"`, `"pittsburgh"`,
`"san_antonio"`, and `"new_york"`.}

\item{start_date, end_date}{Optional date bounds in `YYYY-MM-DD` format or as
`Date` objects.}

\item{last_n_days}{Optional. Positive integer shortcut that sets
`start_date`/`end_date` to the last `n` calendar days ending today.
Cannot be combined with `start_date` or `end_date`.}

\item{offense}{Optional offense description filter applied to
`std_offense_description`.}

\item{offense_category}{Optional offense category filter applied to
`std_offense_category`.}

\item{neighborhood}{Optional neighborhood filter applied to
`std_neighborhood`.}

\item{zip_code}{Optional ZIP/postal code filter applied to `std_zip_code`.}

\item{beat}{Optional police beat filter applied to `std_beat`.}

\item{district}{Optional district/precinct filter applied to `std_district`.}

\item{division}{Optional division/station filter applied to `std_division`.}

\item{disposition}{Optional disposition/closure filter applied to
`std_disposition`.}

\item{limit}{Maximum number of rows to return after local filtering.}

\item{select}{Optional raw source columns to request. Required standardization
columns are added automatically.}

\item{where}{Optional provider-specific filter clause. When supplied for a
city with multiple backing sources, an error is raised.}

\item{output}{Output shape. `"both"` (default) returns source raw columns
plus standardized `std_*` columns. `"comparable"` returns only the
standardized `std_*` columns for multi-city comparisons.}

\item{as_sf}{Logical. If `TRUE`, attempt to convert standardized rows into
an `sf` point object using `std_longitude`/`std_latitude` (EPSG:4326).}

\item{...}{Additional query parameters passed to the provider API
(Socrata or ArcGIS).}
}
\value{
A tibble by default. If `as_sf = TRUE` and valid coordinates are
  available, returns an `sf` object with CRS EPSG:4326.
}
\description{
Downloads incident data for a supported city and appends a common set of
standardized `std_*` columns so downstream code can work across cities.
}
\examples{
\dontrun{
sf_recent <- get_standardized_incidents(
  city = "san_francisco",
  start_date = "2026-04-01",
  end_date = "2026-04-07",
  offense_category = "larceny theft",
  limit = 250
)
}
}
